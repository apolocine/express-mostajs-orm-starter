{
  "name": "express-mostajs-orm-starter",
  "version": "0.1.0",
  "private": true,
  "description": "Express + @mostajs/orm blog starter (SQLite WASM). Boots in the browser / Bolt.new / Cloudflare Workers with no native binary.",
  "license": "MIT",
  "author": "Dr Hamid MADANI <drmdh@msn.com>",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "npm run build && node dist/server.js",
    "start": "npm run build && node dist/server.js"
  },
  "engines": {
    "node": ">=18.18.0"
  },
  "dependencies": {
    "@mostajs/orm": "^2.5.3",
    "express": "^4.21.0",
    "sql.js": "^1.13.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
